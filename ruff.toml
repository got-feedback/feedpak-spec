# Lint config for the reference tooling and tests. The spec prose and schemas
# are not Python; only tools/ and tests/ are linted (see the CI lint job).
target-version = "py310"
line-length = 100

[lint]
select = ["E", "F", "I", "B", "UP", "BLE"]
